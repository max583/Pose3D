# Матрица бэклога: PLAN ↔ рефакторинг DesignDoll

Связка между шагами [`PLAN.md`](../../PLAN.md) и фазами [`refactoring-plan-design-doll-controls.md`](./refactoring-plan-design-doll-controls.md). Обновлять при планировании спринта или смене приоритетов.

**Легенда статусов:** `done` — сделано в коде; `next` — следующий запланированный шаг PLAN; `todo` — в очереди; `optional` — не обязателен для MVP DD.

---

## 1. Шаги PLAN.md (8–11)

| ID | Статус | Зависит от | Блокирует / вклад в DD |
|----|--------|------------|-------------------------|
| Step 8 Mini-view | `next` | — | **DD C1** — глубина для сценария A+B; без мини-вида сложно воспроизвести UX PoseTag |
| Step 9 Multi-skeleton | `todo` | Step 2 (уже есть `skeletons[]`) | **DD D** — несколько фигур; для одного персонажа **optional** |
| Step 10 Center of gravity | `todo` | Step 3 `SkeletonGraph` | **DD B3** — баланс торса как в Practice-туториале |
| Step 11 Ring gizmos | `todo` | Step 8 желателен (общий UX) | **DD C2** — направление «B» у кистей/стоп |

Рекомендуемый порядок по [`PLAN.md`](../../PLAN.md): **8 → 9 → 10 → 11** (Step 9 можно отложить, если цель — только DD для одной фигуры).

---

## 2. Фазы рефакторинга DesignDoll (A–E)

| Фаза | Статус | Зависит от | Связь с PLAN |
|------|--------|------------|----------------|
| **A** Модель данных (фикс длины, лимиты, череп, позвоночник) | `todo` | — | Фундамент; пересекается с устранением stretch в FK ([`STATUS.md`](../../STATUS.md)) |
| **A1** Жёсткая длина кости | `todo` | — | Нет прямого шага PLAN; предпосылка для честного DD |
| **A2** Таблица DOF / joint limits | `todo` | A1 желательно | Предпосылка для Step 11 (кольца с clamp) |
| **A3** Жёсткий череп | `todo` | — | Нет прямого шага PLAN |
| **A4** Виртуальная цепь шея/позвоночник | `todo` | A1, прототип | Нет прямого шага PLAN |
| **B** Семь контроллеров | `todo` | A1 минимум | Пересекается с UX Step 10/11 |
| **C** A + B + мини-вид | `todo` | **C1 ↔ Step 8**, **C2 ↔ Step 11** | См. таблицу ниже |
| **D** Multi-skeleton | `optional` | Step 9 | Полное совпадение |
| **E** UI режимы, документация | `todo` | MVP C + B | После стабилизации |

---

## 3. Сквозная таблица PLAN ↔ DD

| PLAN | DD-фаза | Примечание |
|------|---------|------------|
| Step 8 | **C1** | Мини-вид = обязательная часть модели «глубина без вращения камеры» |
| Step 9 | **D** | Несколько фигур; можно после MVP DD |
| Step 10 | **B3** + сценарий Practice | Перенос центра масс верх/низ |
| Step 11 | **C2** | Кольца / направление B для конечностей |

---

## 4. Рекомендуемая очередь (согласованная)

1. **Step 8** + начало **DD C** (мини-вид в коде).  
2. Параллельно или следом: **DD A1** (фикс длины) — не блокирует Step 8, но нужен для честного позинга.  
3. **Step 10** + **DD B3** (CoG).  
4. **Step 11** + **DD C2** (направление B).  
5. **DD A2–A4**, **B1–B2** — по [`refactoring-plan-design-doll-controls.md`](./refactoring-plan-design-doll-controls.md) §5.  
6. **Step 9** / **DD D** — по необходимости.

*Последнее обновление матрицы: вместе с внедрением P0 инженерных практик.*
