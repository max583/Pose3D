# Шаблон задачи для фичи PoseFlow

Используй этот шаблон перед началом новой пользовательски заметной фичи или крупного изменения поведения. Для Lite mode можно заполнить только разделы 1, 3, 5 и 7.

---

## 1. Название и цель

**Название:**  
**Короткая цель:**  
**Почему это важно для пользователя:**  

---

## 2. Контекст

### Current-state probe

Перед планированием и правками обязательно зафиксируй фактическое состояние проекта. Не опирайся только на `PLAN.md` или прошлые сообщения.

**Команды/проверки:**

- [ ] `git status --short`
- [ ] `STATUS.md`
- [ ] `PLAN.md`
- [ ] `AGENTS.md`
- [ ] релевантные файлы кода через `rg --files`, `git ls-files`, `Get-Content` или аналог
- [ ] если задача затрагивает команды качества: `poseflow/package.json`, `tsconfig*.json`, последняя проверка `npm run verify`

**Что обнаружено:**

- Рабочее дерево:
- Актуальный следующий шаг:
- Реальные файлы/сервисы, которые уже существуют:
- Устаревшие или противоречивые записи в документации:
- Риски для этой задачи:

---

**Актуальные источники:**

- `STATUS.md`:
- `PLAN.md`:
- релевантные файлы кода:

**Текущая архитектурная линия:**

- `React/R3F controllers -> RigService -> SkeletonRig -> resolveSkeleton -> PoseData`.
- Не возвращать старый `updateJoint`/FK-drag путь без отдельного решения.

**Вывод из current-state probe:**

- 

---

## 3. Границы задачи

**Входит в задачу:**

- 

**Не входит в задачу:**

- 

**Файлы/модули, которые можно менять:**

- 

**Файлы/модули, которые не трогаем без отдельной причины:**

- 

---

## 4. Поведение и UX

**Пользовательский сценарий:**

1. 
2. 
3. 

**Ожидаемое поведение:**

- 

**Ручная проверка:**

- [ ] 
- [ ] Если задача затрагивает R3F/UI/drag/export viewport: пройти smoke-чеклист из `ai/docs/r3f-smoke-manual-checklist.md`.

---

## 5. Технический план

**Чистая логика:**

- Где лежит:
- Какие функции/методы:
- Какие edge cases:

**UI/R3F интеграция:**

- Компоненты:
- Pointer/drag поведение:
- Undo/redo:

**Сервисы:**

- `RigService`:
- `PoseService`:
- другие:

---

## 6. Тесты

**Unit-тесты:**

- [ ] happy path:
- [ ] edge case:

**Что не тестируем unit-тестами и почему:**

- 

---

## 7. Acceptance Criteria

- [ ] Фича работает в описанном пользовательском сценарии.
- [ ] Нет unrelated refactor.
- [ ] Новая чистая логика покрыта unit-тестами.
- [ ] Undo/redo работает, если задача меняет позу.
- [ ] `npm run verify` проходит.
- [ ] `CHANGELOG.md` / `STATUS.md` обновлены, если поведение заметно пользователю.
- [ ] ADR обновлён, если принято новое архитектурное решение.

---

## 8. Итог после выполнения

**Изменённые файлы:**

- 

**Проверки:**

- 
- Manual R3F:

**Остаточные риски / follow-up:**

- 
